void PrintMenu(void){
  Serial.println(F("a = test IR LED"));
  Serial.println(F("b = test switches"));
  Serial.println(F("d = test motor direction"));
  Serial.println(F("e =  test motor and sensor values"));
  Serial.println(F("f = test motor and encoder values"));
  Serial.println(F("g = test encoder counting via buttons"));
  Serial.println(F("h = test motor moving to position"));
  Serial.println(F("i = test aim and shoot"));
  Serial.println(F("j = reloader"));
  Serial.println(F("k = demo 1"));
  Serial.println(F("s = serial comms"));
  Serial.println(F("t = targeting"));
  Serial.println(F("v = Demo 4"));
  Serial.println(F("z = print menu"));
 
}